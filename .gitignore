# Dependencies
node_modules/

# Build outputs
target/
dist/
build/
.gradle/
*.hprof

# IDE
.idea/
.vscode/

# Environment
.env
.env.*
!.env.example

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Java
*.class
*.jar
*.war

# Node
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# frontend-maven-plugin managed Node installation
src/frontend/node/

# OpenAPI-generated TypeScript types
src/frontend/src/lib/types/generated/
# KMP domain TypeScript, unpacked by :frontend:unpackDomainTypescript from
# :domain:packageTypescript. Build output — regenerated on every build; must
# not be committed (lives under src/lib only because that is SvelteKit's $lib).
src/frontend/src/lib/domain/
/docs/InfosNotForGit.txt
/src/frontend/test-results/

# Static-analysis reports (detekt for backend/domain land under
# target/ and build/ which are already ignored; ESLint reports
# need an explicit entry)
src/frontend/reports/

# clean-up-project resumable state
planning/cleanup-state.json
/.kotlin/metadata/kotlinTransformedMetadataLibraries/