apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: estimation
spec:
  rules:
    - host: estimation.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
