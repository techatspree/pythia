node_modules
.svelte-kit
build
.git
*.log
# Generated by the `predev` npm script for local dev (task-162). The Dockerfile
# does `COPY . .`, so without this a developer who ran `npm run dev` and then
# `docker build` would bake a dev-auth config into the image — and it would win
# wherever no ConfigMap overrides it.
static/config.json
