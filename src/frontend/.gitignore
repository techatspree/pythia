node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.test

# Runtime config generated by the `predev` npm script (task-162). Never
# committed and never baked into the image (see .dockerignore);
# static/config.example.json documents the shape.
static/config.json

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
