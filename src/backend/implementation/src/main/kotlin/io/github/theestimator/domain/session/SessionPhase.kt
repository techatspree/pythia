package io.github.theestimator.domain.session

/** The two phases of the Wideband-Delphi flow: a blind individual estimate
 *  (PHASE1) followed by a discussion + revised estimate (PHASE2). */
enum class SessionPhase {
    PHASE1,
    PHASE2
}
